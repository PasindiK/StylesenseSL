# --- Local backends (http://localhost — use with `npm run dev`) ---
VITE_API_URL=http://localhost:8000/api
VITE_AGENTIC_API_URL=http://localhost:8000/api
VITE_DATA_MESH_API_URL=http://localhost:8001
VITE_DATA_FABRIC_API_URL=http://localhost:8002/api
VITE_DATA_ARCH_API_URL=http://localhost:8003/api

# --- Vercel + HTTPS (no mixed content): same-origin /re/... proxies ---
# Copy from .env.vercel.example — also configure Vercel dashboard with those values.
