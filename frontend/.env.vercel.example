# Vercel Production: copy these to Project → Settings → Environment Variables (or use for local `vite build` tests).
# Requires `vercel.json` rewrites in this folder. Update rewrites when Azure IPs change.

VITE_API_URL=/re/agentic
VITE_AGENTIC_API_URL=/re/agentic
VITE_DATA_MESH_API_URL=/re/mesh
VITE_DATA_FABRIC_API_URL=/re/fabric
VITE_DATA_ARCH_API_URL=/re/arch
